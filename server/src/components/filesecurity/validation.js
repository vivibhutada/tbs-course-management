const joi = require('joi');
const {InputDateFormat} = require('../../config/constant');


const messages = {
  "failed_decode":"Failed to send decrypted file.",
  
};



module.exports = {
  
  messages

};

